import React from "react";
import "../App.css";

const SearchBar = () => {
  return (
    <form action="#" className="row space-evenly">
      <input type="text" placeholder="search restaurants" />
      <input type="submit" />
    </form>
  );
};

export default SearchBar;
